# GetItDone

Inspired by the problems of day to day lives, this system finds volunteers to get your most mundane things
done.

## Technologies
`Ionic`, `Angular 7`. <br>
`Firebase`. <br>
`Google-maps`, `Geo Fencing`.
